# Contribuer

```powershell
npm run install:app
npm run dev
npm test
npm run build
```

Pour un bug de difference entre l'app et le jeu, fournir le JSON exporte par l'app et une capture du tooltip **Room Details** en jeu.
